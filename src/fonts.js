import { Inter, Cinzel, Aldrich, Tektur, Playfair_Display } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
  })
   
export const cinzel = Cinzel({
  subsets: ['latin'],
  display: 'swap',
})

export const aldrich = Aldrich({
  weight: ["400"],
  subsets: ['latin'],
  display: 'swap',
})

export const tektur = Tektur({
  subsets: ['latin'],
  display: 'swap',
})

export const playfairDisplay = Playfair_Display({
  subsets: ['latin'],
  display: 'swap',
})